## Packages
framer-motion | Complex page transitions and UI animations
recharts | Analytics charts for dashboard
date-fns | Date formatting for deadlines and timestamps
clsx | Utility for constructing className strings conditionally
tailwind-merge | Utility for merging Tailwind classes efficiently

## Notes
Authentication uses Replit Auth (use-auth.ts hook provided).
API follows standard REST patterns defined in shared/routes.ts.
Sidebar layout required for main navigation.
Dashboard needs role-based views (Ops vs Installer vs Engineer).
Job detail view needs tabs for context switching.
